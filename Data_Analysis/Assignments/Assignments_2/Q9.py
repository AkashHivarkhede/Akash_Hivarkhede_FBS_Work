# Use NumPy's built-in functions to compute the sum, mean, and standard deviation of a
# given array.

import numpy as np

arr = np.array([10 , 20 , 30 , 40 , 50])

print('Sum :' , np.sum(arr))

print('Mean :' , np.mean(arr))

print('Standard Diviation :' , np.std(arr))